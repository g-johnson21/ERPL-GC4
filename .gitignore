# Recorded test data — keep the folder, not the runs.
data/*.csv
data/*.meta.json

# Timestamped config backups written on every save from the Config page.
config/backups/

# Per-machine hardware wiring (COM port, chassis name, channel maps).
# config/hardware.example.json is the tracked template.
config/hardware.json

node_modules/
*.log
.DS_Store
Thumbs.db

# Python bytecode from the DAQ sidecar.
__pycache__/
